# PLAN — GeoCheckr Berlin Edition (19.06.2026)

## Überblick

Ziel: Aus dem aktuellen `geo_Berlin` Branch eine funktionale App machen, die Berliner Panoramen (AVIF) statt Google Street View nutzt, mit ArUco-Marker-Scanner, Timer-Slider und deutschem UI.

---

## 1. Daten: `src/data/panoramaLocations.ts`

**Aktuell:** 205 Weltstädte mit `city/country/lat/lng/continent`

**Neu:** 39 Berliner Locations mit Interface:

```typescript
export interface PanoramaLocation {
  id: number;
  name: string;        // Straßenname (z.B. "Brandenburger Tor")
  district: string;    // Bezirk (z.B. "Mitte")
  lat: number;
  lng: number;
  url_avif: string;    // https://timoboese.com/pano/berlin/ID.avif
  qr_data: string;     // JSON: {"id":1,"name":"Brandenburger Tor","pano":"berlin_01"}
}
```

**Quelle:** FTP-Server `timoboese.com/pano/berlin/` — dort liegen AVIF-Dateien.

**Referenz:** `Geo_Berlin_260608.md` enthält die Liste der 39 Locations.

---

## 2. PanoramaViewer: `src/components/PanoramaViewer.tsx`

**Neue Komponente** — ersetzt `buildStreetViewHtml()` + WebView.

**Technik:** WebView mit Pannellum CDN (leichtgewichtig, 360°-Panorama-Viewer).

**HTML-Struktur:**
```html
<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/pannellum@2.5.6/build/pannellum.css"/>
  <script type="text/javascript" src="https://cdn.jsdelivr.net/npm/pannellum@2.5.6/build/pannellum.js"></script>
  <style>
    html, body, #panorama { width: 100%; height: 100%; margin: 0; padding: 0; overflow: hidden; background-color: #000; }
  </style>
</head>
<body>
  <div id="panorama"></div>
  <script>
    pannellum.viewer('panorama', {
      "type": "equirectangular",
      "panorama": "AVIF_URL",
      "autoLoad": true,
      "showControls": false
    });
  </script>
</body>
</html>
```

**Props:**
- `url: string` — AVIF-URL
- `onLoad: () => void` — wird aufgerufen wenn Panorama geladen
- `onError: () => void` — bei Fehler

**Verwendung in App.tsx:**
```tsx
<PanoramaViewer
  url={location.url_avif}
  onLoad={() => setSvLoaded(true)}
  onError={() => setSvError(true)}
/>
```

---

## 3. Timer-Slider: Aus `origin/master` (App.tsx.fallback)

**Quelle:** `remotes/origin/master:App.tsx.fallback` (Zeilen 884-975)

**SliderTrack-Komponente** — selbstgebaut mit PanResponder, kein React Native Slider.

**Snap-Werte:** `[1, 5, 10, 20, 30, 45]` Sekunden

**Funktionsweise:**
- Touch-gesteuerter Track mit farbigem Balken
- Kreis-Puck folgt dem Finger
- Labels unter dem Track sind klickbar
- `onChange(value)` gibt den gesnappten Wert zurück

**Einbau in App.tsx Setup-Screen:**
```tsx
<View style={s.sectionLabel}><Text style={s.sectionLabelText}>TIMER</Text></View>
<SliderTrack value={timerSetting} onChange={setTimerSetting} min={1} max={45} />
```

**Runden bleiben als Chips** (5/10/15) — kein Slider nötig.

---

## 4. TextRecognition: `@react-native-ml-kit/text-recognition`

**Quelle:** `remotes/geocheckr/main:package.json` — `"@react-native-ml-kit/text-recognition": "^2.0.0"`

**Einbau in App.tsx:**
```tsx
import TextRecognition from '@react-native-ml-kit/text-recognition';
```

**OCR-Logik für ArUco-Marker:**
- Kamera scannt Marker
- `TextRecognition.recognize(photo.uri)` extrahiert Text
- Text wird gegen `qr_data` der Locations gematched
- Bei Match → Location wird geladen

**Scanner-Modi:**
1. **City-Scan** (Setup): Spieler scannt seine Stadt-Karte → OCR erkennt Stadtname → Player zugewiesen
2. **Game-Scan** (Spiel): Spieler scannt ArUco-Marker → OCR erkennt `qr_data` → Panorama wird geladen

---

## 5. Tie-Breaker: Aus `remotes/geocheckr/main:App.tsx`

**Quelle:** `geocheckr/main` App.tsx (Zeilen 834-847)

**Logik:**
```typescript
const startTieBreaker = () => {
  setRound(round + 1);
  setMaxRounds(maxRounds + tiePlayers.length);
  setPhase('scan-qr');
  setScreen('game');
  setActivePlayerIdx(0);
  startRound();
};
```

**Einbau:** Im End-Screen, wenn `isTie === true` → "TIE BREAKER — PLAY ON!" Button anzeigen.

---

## 6. ArUco-Block (eigene Stadt blockieren)

**Quelle:** `remotes/geocheckr/main:App.tsx` (Zeilen 211-223, `onQrScanned`)

**Angepasst für Berlin:**
```typescript
const onQrScanned = useCallback((loc: PanoramaLocation) => {
  // Block player start cities
  const playerCity = players.find(p => p.city.toLowerCase() === loc.name.toLowerCase());
  if (playerCity) {
    setQrBlockedMsg('Diese Stadt ist bereits auf dem Tisch!');
    Vibration.vibrate(300);
    setTimeout(() => setQrBlockedMsg(''), 2000);
    return;
  }
  setLocation(loc);
  setUsedLocations(prev => [...prev, loc.id]);
  setTimer(timerSetting);
  setTimerPaused(false);
  setPhase('view');
  setShowQrScanner(false);
  setScanned(false);
  Vibration.vibrate(100);
}, [timerSetting, players]);
```

---

## 7. "Load New Card" nach 5s Timeout

**Quelle:** `remotes/geocheckr/main:App.tsx` (Zeilen 137-141, 225-231)

**Logik:**
```typescript
// SV loading timeout — show LOAD NEW CARD after 5s
useEffect(() => {
  if (phase !== 'view' || svLoaded || svError) { setSvLoadingLong(false); return; }
  const t = setTimeout(() => setSvLoadingLong(true), 5000);
  return () => clearTimeout(t);
}, [phase, svLoaded, svError, location]);

const loadNewCard = useCallback(() => {
  const newLoc = getRandomLocation();
  setLocation(newLoc);
  setUsedLocations(prev => [...prev, newLoc.id]);
  setSvLoaded(false); setSvError(false); setSvLoadingLong(false);
  setTimer(timerSetting); setTimerPaused(false);
}, [getRandomLocation, timerSetting]);
```

---

## 8. Dateien & Änderungen

| Datei | Aktion | Beschreibung |
|-------|--------|-------------|
| `src/data/panoramaLocations.ts` | **ERSETZEN** | 39 Berliner Locations mit AVIF-URLs |
| `src/components/PanoramaViewer.tsx` | **NEU** | Pannellum-basierter 360°-Viewer |
| `App.tsx` | **BEARBEITEN** | Slider, PanoramaViewer, TextRecognition, Tie-Breaker, ArUco-Block, Load New Card |
| `package.json` | **BEARBEITEN** | `@react-native-ml-kit/text-recognition` hinzufügen |

---

## 9. Todo-Liste

- [ ] `src/data/panoramaLocations.ts` — 39 Berliner Locations einpflegen
- [ ] `src/components/PanoramaViewer.tsx` — Pannellum-Komponente erstellen
- [ ] `App.tsx` — Timer-Slider aus `origin/master` einbauen
- [ ] `App.tsx` — PanoramaViewer statt Street View WebView
- [ ] `App.tsx` — TextRecognition import + OCR-Logik
- [ ] `App.tsx` — Tie-Breaker aus `geocheckr/main`
- [ ] `App.tsx` — ArUco-Block (eigene Stadt blockieren)
- [ ] `App.tsx` — "Load New Card" nach 5s
- [ ] `package.json` — `@react-native-ml-kit/text-recognition` hinzufügen
- [ ] Build testen (GitHub Actions / lokal)

---

## 10. Referenzen

- **Aktueller Branch:** `geo_Berlin` (Commit `6991ac4e64aa4daeed0c41a321eae338a86b3c4f`)
- **Slider-Quelle:** `remotes/origin/master:App.tsx.fallback` (Zeilen 884-975)
- **Tie-Breaker-Quelle:** `remotes/geocheckr/main:App.tsx` (Zeilen 834-847)
- **QR-Block-Quelle:** `remotes/geocheckr/main:App.tsx` (Zeilen 211-223)
- **Load-New-Card-Quelle:** `remotes/geocheckr/main:App.tsx` (Zeilen 137-141, 225-231)
- **TextRecognition-Quelle:** `remotes/geocheckr/main:package.json`
- **Panorama-Daten:** `Geo_Berlin_260608.md` + FTP `timoboese.com/pano/berlin/`
- **Pannellum CDN:** `https://cdn.jsdelivr.net/npm/pannellum@2.5.6/build/pannellum.js`
