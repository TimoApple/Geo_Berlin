# STATUS 21.06.2026 – ArUco Scanner Debug

## Problem
ArUco-Scanner-Code wird nie ausgeführt. Keine `[ArUco]` Logs im logcat.
Die App auf dem Handy hat den ALTEN Code (vor Continuous Scanning + Debug-Logs).

## Bereits gemacht
1. **`arucoDetector.ts`** – Dictionary von `ARUCO` (7x7) auf Default `ARUCO_MIP_36h12` (6x6) umgestellt
2. **`useArucoScanner.ts`** – Continuous Scanning Loop eingebaut (scanCard alle 500ms)
3. **`useArucoScanner.ts`** – Überall `console.log('[ArUco] ...')` eingebaut:
   - Hook-Initialisierung
   - `setIsActive` Aufruf (wrapped)
   - `scanCard` Start/Abbruch
   - Foto aufnehmen
   - Resize
   - Base64 lesen
   - JPEG decodieren
   - Graustufen
   - Marker-Detektion
   - Scan-Loop Start/Stopp
   - Alles in try/catch gewrapped
4. **`App.tsx`** – `setIsActive(true/false)` beim Öffnen/Schließen des Scanners
5. **`PanoramaViewer.tsx`** – Einfaches Image-Load statt Pannellum

## Nächste Schritte (morgen)
1. Build-Status checken: https://github.com/TimoApple/Geo_Berlin/actions
2. APK herunterladen (Artifact: `app-release.apk`)
3. APK aufs Handy installieren:
   ```
   adb install -r path/to/app-release.apk
   ```
4. logcat starten:
   ```
   adb logcat -c
   adb logcat | findstr /i "aruco"
   ```
5. App öffnen → "KARTE" tippen → ArUco-Marker vor Kamera halten
6. logcat Output analysieren – wo genau hängt es?
   - `[ArUco] Hook initialisiert` → wird der Hook überhaupt geladen?
   - `[ArUco] setIsActive aufgerufen mit: true` → wird der Scanner aktiviert?
   - `[ArUco] Scan-Loop läuft...` → startet die Loop?
   - `[ArUco] scanCard aufgerufen` → wird scanCard getriggert?
   - `[ArUco] Foto wird aufgenommen...` → klappt takePictureAsync?
   - `[ArUco] Marker gefunden:` → werden Marker erkannt?

## Git
- Letzter Commit: `ab7f284` – "DEBUG: ArUco Scanner mit console.logs an jeder Station"
- Branch: `master` → gepusht zu `geo_berlin/main`
- Build getriggert (2x, neuester ist der relevante)
