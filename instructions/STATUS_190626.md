# STATUS 19.06.2026 23:00

## Letzter Commit
`f9a5066` auf `geo_berlin/master` — pushed, Build läuft

## Build-Status
- **Letzter erfolgreicher Build:** #27842112297 (20:23 Uhr)
- **Aktueller Build:** läuft gerade → https://github.com/TimoApple/Geo_Berlin/actions

## Was geändert wurde (seit letztem erfolgreichen Build)

### ✅ App-Icon
- `app.json` → `icon: "./assets/GeoChecker_Aruco_Round_01.png"`
- Setup-Screen → `GeoChecker_Aruco_Square_01.png` als Logo

### ✅ Tutorial
- ScrollView mit `pagingEnabled`
- Swipe + Dots mittig
- Klick auf Button zum Weiter

### ✅ Setup-Screen
- Keine Linien unter Labels (`borderBottomWidth` entfernt)
- Location-Buttons gleiche Höhe wie Input-Felder (`borderWidth: 1`)
- Timer default: 15 → 10 Sekunden

### ✅ OCR / ArUco-Scanner
- Iteriert über `result.blocks`
- Nimmt nur ersten Treffer (keine rote Liste mehr)

### ✅ Workflow
- `master` zur Branch-Liste in `build-apk.yml` hinzugefügt

### ❌ Build-Fehler (behoben)
- `intro_alt.mp4` und `intro_lang.mp4` wurden in Commit `af2f6bb` versehentlich committed
- Expo prebuild → Jimp stürzt über `.mp4` ab (`Unsupported MIME type: audio/mpeg`)
- **Fix:** `git rm --cached` + Commit `f9a5066` — Dateien aus Repo entfernt
- `assetBundlePatterns` wieder auf `"**/*"` (wie vorher)

## Nächster Schritt
- Build abwarten (~15 Min)
- APK von GitHub Actions runterladen
- Auf Android installieren und testen
